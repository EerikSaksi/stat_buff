mutation createWorkoutPlan($name: String!, $appUserId: Int!) {
  createWorkoutPlan(
    input: {workoutPlan: {name: $name, appUserId: $appUserId}}
  ) {
    workoutPlan {
      ...WorkoutPlan
    }
  }
}
