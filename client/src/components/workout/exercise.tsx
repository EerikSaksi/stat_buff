import React, { useState } from "react";
import { List, } from "react-native-paper";
import WorkoutExerciseSet from "./exercise_set";

const WorkoutExercise: React.FC<WorkoutPlanExerciseFragment> = ({ }) => {
  return (
  );
};
export default WorkoutExercise; 
